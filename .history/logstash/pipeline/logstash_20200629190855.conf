input {
	redis {
		id => "RedisLog"
		batch_count => 1
		data_type => "list"
		key => "kg:basic:log"
		host => "172.19.0.3"
		port => 6379
		password => "root"
		db => 6
		threads => "5"
    }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		index => "kg-log"
	}
}
